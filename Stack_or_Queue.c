#include "monty.h"

/**
 * is_stack - LIFO
 * @stack: the stack
 * @line_number: number of lines
 */

void is_stack(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
	idx = 1;
}

/**
 * is_queue - FIFO
 * @stack: the stack (is a queue)
 * @line_number: number of lines
 */

void is_queue(stack_t **stack, unsigned int line_number)
{
	UNUSED(stack);
	UNUSED(line_number);
	idx = 0;
}
